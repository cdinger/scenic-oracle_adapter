name: CI

on: [push]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Start containers
      run: docker-compose up -d
    - name: Check containers
      run: docker-compose ps
    - name: Run tests
      run: docker-compose exec gem bin/test
